[tool.pytest]
minversion = "9.0.2"
log_level = "WARNING"
log_file = "../test_data/pytest_acts.log"
addopts = [
    "--generator=acts",
    "--capture=tee-sys",
    "--cache-clear",
    "--maxfail=15",
]
testpaths = [
    "tests",
]
python_files = ["test_main.py"]